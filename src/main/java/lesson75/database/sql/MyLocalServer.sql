Create table man (
    id int NOT NULL,
    age int NOT NULL,
    name varchar (20) NOT NULL,
    surname varchar (20) NOT NULL,
    width int NOT NULL,
    height int NOT NULL,
    sex boolean NOT NULL DEHAULT '0'
    desription text
);
